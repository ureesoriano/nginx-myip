server {
  listen 80 default_server;
  listen [::]:80 default_server;

  server_name _;

  # / : return 403 && hide server info
  location / {
    deny all;
    server_tokens off;
  }

  location /ip {
    default_type text/plain;
    return 200 $remote_addr\n;
  }
}
